/**
 * Room Response DTO
 *
 * YAGNI: Only what we need for current use case
 */

import type { IRoom } from '../../entities/room';

/**
 * Room response - just the entity
 */
export type IRoomResponseDto = IRoom;
