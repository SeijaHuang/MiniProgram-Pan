/**
 * User Response DTO
 * 
 * YAGNI: Only what we need for current use case
 */

import type { IUser } from '../../entities/user';

/**
 * User response - just the entity
 */
export type IUserResponseDto = IUser;
