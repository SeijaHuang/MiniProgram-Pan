/**
 * Message Response DTO
 * 
 * YAGNI: Only what we need for current use case
 */

import type { IMessage } from '../../entities/message';

/**
 * Message response - just the entity
 */
export type IMessageResponseDto = IMessage;
