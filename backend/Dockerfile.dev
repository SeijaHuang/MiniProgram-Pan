# 使用 Node.js 官方镜像
FROM node:18-alpine

# 设置工作目录
WORKDIR /app

# 复制 package.json 和 package-lock.json
COPY package*.json ./

# 安装所有依赖 (包括开发依赖)
RUN npm install

# 复制源代码
COPY . .

# 暴露端口
EXPOSE 8080

# 设置环境变量
ENV NODE_ENV=development

# 启动开发服务器 (支持热重载)
CMD ["npm", "run", "dev"]
